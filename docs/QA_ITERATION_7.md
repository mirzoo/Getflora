# QA Iteration 7: Photo Upload

Дата: 2026-06-01.

## Результат

Status: passed.

## Проверено автоматически

Backend/storage lifecycle:

1. Загружены 3 тестовых PNG в MinIO.
2. Создано тестовое объявление в PostgreSQL с 3 `ListingImage`.
3. Все 3 public URL вернули `200`.
4. Сымитировано редактирование:
   - 1 старое фото оставлено;
   - 1 новое фото добавлено;
   - 2 старых фото удалены из набора.
5. В базе после редактирования осталось 2 фото.
6. Удаленные MinIO-файлы вернули `404`.
7. Оставленное и новое фото вернули `200`.
8. Тестовое объявление и тестовые файлы удалены.

UI checks:

1. Главная страница открывается без runtime overlay.
2. Форма создания содержит фото-пикер:
   - кнопка `Добавить фото`;
   - скрытый file input;
   - лимит `Максимум 10 фото`;
   - textarea ссылок на фото не показывается.
3. Форма редактирования содержит фото-пикер:
   - кнопка `Добавить фото`;
   - превью текущих фото;
   - кнопки `Удалить фото`;
   - textarea ссылок на фото не показывается.

## Что проверить руками

- Создать объявление с 1 фото.
- Создать объявление с 3-5 фото.
- В редактировании добавить фото.
- В редактировании удалить фото.
- Проверить, что карточка и модалка показывают итоговый набор фото.
